{
  email your-email@example.com   # 인증서 만료 알림용(선택)
}

ws.noelkim12.dev {

  # WebSocket 엔드포인트만 프록시하고 싶다면 경로 매칭
  @ws path /ws*
  reverse_proxy @ws 127.0.0.1:13131

  # (선택) 응답 압축
  encode zstd gzip
}